"""Developer scripts (seed, persistence, graphify, caveman-fix).

Packaged so app/ and tests/ can `import scripts.*` from an installed
distribution (CI installs the package and runs bare pytest with no CWD
on sys.path).
"""
